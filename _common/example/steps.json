[
  {
    "reqKick":{
      "steps":[
        {
          "group":"boot",
          "script":"docker run -d --name <buildJobId>.pre_task busybox sh -c 'sleep 10; ls'"
        },
        {
          "group":"wait_exec",
          "script":"docker wait <buildJobId>.pre_task"
        }
      ],
      "name":"Setup"
    },
    "reqExec":{
      "steps":[
        {
          "group":"task",
          "script":"some user defined script steps"
        }
      ],
      "name":"Setup"
    }
  },
  {
    "reqKick":{
      "steps":[
        {
          "group":"task",
          "script":"some user defined script steps"
        },
        {
          "group":"wait_exec",
          "script":"docker wait <buildJobId>.pre_task"
        }
      ],
      "name":"Task 2"
    },
    "reqExec":{
      "steps":[
        {
          "group":"task",
          "script":"some user defined script steps"
        }
      ],
      "name":"Task 2"
    }
  }
]
